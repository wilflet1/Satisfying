{
  "name": "quicksilver",
  "version": "0.1.0",
  "description": "Split to fit, merge to smash — a liquid-metal one-thumb runner.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "typecheck": "tsc --noEmit",
    "playtest": "node scripts/playtest.mjs",
    "artifact": "npm run build && node scripts/bundle-artifact.mjs",
    "sim-test": "node --experimental-strip-types scripts/sim-test.mjs",
    "net-test": "node --experimental-strip-types scripts/net-test.mjs",
    "server": "node --experimental-strip-types server/node.ts"
  },
  "repository": {
    "type": "git",
    "url": "http://local_proxy@127.0.0.1:41729/git/wilflet1/Satisfying"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "ws": "^8.21.2"
  },
  "private": true,
  "type": "module",
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "playwright": "^1.62.1"
  }
}
